package ge.nick.sqlutils;

/**
 * Enum that contain type of ordering of data for ORDER BY {column_name} command.
 * ASC - ascending order.
 * DESC - descending order.
 */
public enum OrderDirection {
    ASC, DESC
}
