# Builder design pattern
Builder design pattern on java in SQL API example.

Using: IntelJ IDEA Community Edition

## Task
Write an example of the builder pattern:
A builder that assembles some complex class.

It can be multifunctional
Home builder, SQL command (for example, Select) object builder, machine etc.
